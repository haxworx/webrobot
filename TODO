* Add a basic search tool for parsing results from the data.
* Write scripts for setting SQL user rights.
* Add more comments.
* Add AWS auth to the Symfony UI.
* Add a script to setup seperate permissions for the crawler and UI (SQL).
* Make the web logging better.
* Think about reintroducing MQTT into the Symfony UI.
